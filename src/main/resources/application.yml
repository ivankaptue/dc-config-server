server:
  port: 9296

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ivankaptue/demo-microservice-daily-code-config-server
          clone-on-start: true
